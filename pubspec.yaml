name: irisesce
description: iOS ArtCNN FlutterTexture player prototype.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  artcnn_player_ios:
    path: packages/artcnn_player_ios

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  pigeon: ^22.7.0

flutter:
  uses-material-design: true
